# Changelog

## 0.0.1a0 — alpha skeleton

- `stepscope.init(local=True)` writes to a local SQLite DB
- `with stepscope.step("name"):` records a step with start/end/status
- `stepscope funnel <db>` renders an ASCII funnel chart
